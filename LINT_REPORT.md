# LINT_REPORT.md — Weekly wiki health check (2026-07-20)

Generated by the automated weekly lint pass. Items below were flagged but **not** auto-fixed because they require judgment. Review each and decide: edit the page, create the missing page, or dismiss.

Auto-fixes applied in this same pass:
- `wiki/projects/helm.md` line 425 — removed broken `[[wiki/concepts/]]` blank wikilink; replaced with plain text "a candidate concept page (name pending approval before filing; see log 2026-07-13)".
- `index.md` stats line — updated `last updated 2026-06-17` → `last updated 2026-07-13` with an explanatory parenthetical pointing here.

---

## 1. Broken wikilinks requiring judgment

### `wiki/projects/helm.md` — blank concept link (concept name never chosen)

The 2026-07-13 log entry records: *"Flagged a concept-page candidate — 'helpful-assistant theater' (a weak model fabricating progress instead of invoking tools) — awaiting explicit approval before filing."* The auto-fix pass removed the broken `[[wiki/concepts/]]` syntax (which caused the script error) but **did not create the concept page** — that awaits explicit approval.

**Action needed**: If you want the concept page, say "file helpful-assistant-theater concept page" in a maintainer-mode session. If you don't want it, no action needed (the auto-fix already cleaned up the broken link).

---

## 2. Stale content in `wiki/projects/helm.md`

The helm.md page was last updated 2026-07-13 and the Current Focus + Architecture Decisions sections are current. However, several earlier sections now contradict the recorded decisions:

### 2a. "Why internal-only" section (lines 31–33) — superseded by Decision #20

The section still reads *"Why internal-only: Godwin is the only user. No multi-tenant data scoping, no RBAC…"*. Decision #20 (recorded in the Architecture Decisions table) explicitly marks this as "reverses the internal-only, single-user, no-RBAC decision #9". The Risks section's "Single-user assumption" item (line 432) also became moot after D1–D2 shipped RBAC.

**Action needed**: Annotate the "Why internal-only" section with a dated reversal note (e.g. *"⚠ Superseded by Decision #20 (2026-07)"*) and mark the "Single-user assumption" risk as resolved. Or rewrite those sections. Judgment call on how much prose to retain vs. replace.

### 2b. "Definition of done for v1" (lines 312–320) — references the old fixed 6-agent GTM roster

The DoD still says "All 6 agents are running in production — Lead Mgmt + Sales + Marketing + Ops + PM + Analytics". The platform pivoted to a generic `Org → Workspace → Agents` abstraction (Decision #13); the fixed roster became example agents, not hard-wired code.

**Action needed**: Update the DoD to reflect the current definition of "shipped" for the generic platform. Or annotate it as the original v1 framing that is still aspirationally valid but no longer tied to specific named agents.

### 2c. "Related entities" missing Composio

`[[wiki/entities/composio]]` is not listed under Related entities despite being central to Decision #21 (Composio hosted-MCP for per-workspace tool integrations).

**Action needed**: Add `- [[wiki/entities/composio]] — hosted-MCP tool integrations (Gmail / Google Docs / Firecrawl / 1000+ toolkits); central to decision #21.` to the Related entities section.

---

## 3. Stale index.md project entries

The automated script reported 0 index drift (no missing or extra pages), but the **one-line summaries** for two active projects are semantically stale relative to the project pages:

### 3a. Helm entry (index.md line ~1310)

Current index entry: *"…repos now pushed to `github.com/godwin-roam/{…}` (not the originally-planned `ROAM-Labs` org); FastAPI … **pivoted from the fixed 6-agent GTM roster to a generic Org → Workspace → Agents platform**…; one blocker — OpenRouter 402 (no credits)…; reusable blueprint at [[wiki/syntheses/multi-agent-ops-platform-blueprint]]…"*

What's stale:
- Repos are now at `github.com/ROAMLABS-Technologies/…` (Decision #23, 2026-07-09). The entry still says `godwin-roam`.
- The OpenRouter 402 blocker is **resolved** (key funded, per Decision #20 current-focus section).
- The entry says "single user (Godwin)" but Helm is now a multi-tenant SaaS (D1–D5 shipped).
- "build active 2026-06-26" should be updated to reflect 2026-07-13 state.

**Action needed**: Rewrite the Helm index entry to reflect current state (multi-tenant SaaS, ROAMLABS-Technologies org, deployed to prod, agents running live).

### 3b. Kivora entry (index.md line ~1311)

Current index entry only captures state through 2026-05-17 ("Systems Inventory v1 shipped 2026-05-16/17"). The 2026-07-13 update-project log entry records 5+ additional weeks: Modules, AI Spend Tracker, multi-channel notifications, Compass 292 tools (effectively 100% GRC coverage), custom connectors, sub-agent autonomy, chat UX overhaul, security hotfixes, Railway blocker.

**Action needed**: Refresh the Kivora index summary to capture the current state (GRC module effectively complete, agent autonomy shipped, Compass at 292 tools).

---

## 4. Stale synthesis: `wiki/syntheses/helm-commercialization-paths`

The synthesis (created 2026-05-09) records Helm as "internal-only for v1" with the multi-tenant marketplace shape explicitly deferred. Decision #20 in helm.md (2026-07) records the actual reversal: Helm became a productized multi-tenant SaaS, which cashed in the `tenant_id`-from-day-1 insurance.

The prior 2026-06-08 semantic lint pass **did not flag this synthesis as stale** — at that time Decision #20 hadn't happened yet. It needs an annotation now, consistent with the pattern applied to [[wiki/syntheses/godwin-portfolio]] in that same pass.

**Action needed**: Add a dated ⚠ Stale annotation to helm-commercialization-paths (similar to the godwin-portfolio annotation from 2026-06-08) noting that the re-evaluation trigger was invoked and the multi-tenant pivot executed as Decision #20. Bump `updated:` to 2026-07-20.

---

## 5. Missing concept page: "helpful-assistant theater" (or similar)

As described in item 1, this concept was flagged in the 2026-07-13 log but never filed. It describes a pattern worth capturing in the wiki: *a weak model, given tools it won't reliably invoke, fabricates confident progress reports instead of doing the work — "sycophantic simulation" failure mode.* It generalizes to any agentic system where model tier ≠ task complexity.

**Action needed**: Approve filing and say "file helpful-assistant-theater concept page" in a maintainer-mode session. Suggested slug: `wiki/concepts/helpful-assistant-theater.md`. References: helm.md lesson section (2026-07-13); also relates to [[wiki/concepts/agent-evals]] and [[wiki/concepts/reasoning-execution-split]].

---

## 6. Long-stale stubs with high inbound link counts (from prior lint — no new findings)

The 2026-06-08 semantic pass surfaced these as "load-bearing but thin expansion candidates". Surfaced again for visibility — no new findings, no change in priority:

- `[[wiki/entities/coldiq]]` — now de-stubbed (30 Jun backfill). ✅ Resolved.
- `[[wiki/entities/skill-md]]` (28 inbound links) — still a stub; expansion requires ingesting an Anthropic SKILL.md primary source.
- `[[wiki/entities/anthropic-skills]]` (21 inbound links) — still a stub.
- `[[wiki/entities/model-context-protocol]]` (18 inbound links) — still a stub.
- `[[wiki/entities/openrouter]]` (15 inbound links) — still a stub.

These are **intentional stubs** that self-document why they're thin. No action required unless a primary source becomes available.

---

## 7. Synthesis `helm-voice-profiles` — soft migration flag

The 2026-05-09 synthesis note says *"will migrate from `wiki/syntheses/` to `helm-repo/voice-profiles/` when the Helm repo exists."* The Helm repos now exist and are deployed. The profiles themselves may be stale (originally extracted from landing-site copy; Helm has since built a Marketing agent).

**Action needed**: Decide whether to migrate the voice profiles to the Helm repo now, and/or refresh them from current landing-site copy. Low urgency — no consistency defect, just a dated migration flag.

---

*End of LINT_REPORT.md. Total items: 7 categories. Auto-fixes in this pass: 2. Human-review items: 7.*
