# LINT_REPORT.md — Weekly Wiki Health Check (2026-05-25)

Generated by the weekly lint pass. Items in this file were **not** auto-fixed and require human review or an explicit opt-in to create new content.

Automated script (`bin/wiki_lint.py`): **clean** — 0 broken wikilinks, 0 ambiguous links, 0 orphan pages, 0 index drift, 0 frontmatter type issues across 459 files (455 wiki pages).

---

## Auto-fixes applied (no human action needed)

| File | Change | Reason |
|---|---|---|
| `wiki/projects/helm.md` | `updated: 2026-05-09` → `updated: 2026-05-10` | Git mtime: most recent commit to this file is `2026-05-10 19:14:39 +0000` ("Hermes Agent blocker #1 cleared"). Frontmatter was one day behind. |
| `index.md` | stats line "last updated 2026-05-11" → "last updated 2026-05-19" | Wiki last changed on 2026-05-19 (Kivora Tier 2A commit). Stats line was 8 days stale. |

---

## 1. Contradictions

### C1 — `wiki/projects/glydr.md`: stale repo count in Stack section

**Severity**: Medium (navigational confusion, not incorrect project facts)

The "Stack and infrastructure" section header (line 37 of the file) reads:

> Workspace folder + **four** sibling repos, each with its own toolchain.

And the Repo topology table lists only four repos (`glydr_backend`, `glydr_landing`, `glydr_cc`, `glydr_mobile`).

However, every other part of the page uses **five**:
- Frontmatter `repo:` field: *"5 separate repos — glydr_backend / glydr_landing / glydr_cc / glydr_mobile / **glydr_docs**"*
- Page intro note (dated 2026-05-15): *"workspace folder + **5** sibling repos"*
- Open question Q13: *"only `BRAIN.md` + 5 sibling clones remain at the root"*
- External links section: includes `https://github.com/kobbyopoku/glydr_docs`

The `glydr_docs` repo (holds PRD, phase docs, ADRs, brand docs) was carved out ~2026-05-15 when the workspace root was slimmed. The Stack section was never updated to reflect the 5th repo.

**Recommended fix**: Update "Workspace folder + four sibling repos" → "Workspace folder + five sibling repos" and add `glydr_docs` to the Repo topology list with a one-line description (e.g., *PRD + phase docs + ADRs + brand; formerly at workspace root, became separate repo ~Phase 7*).

---

## 2. Stale claims

None found. Model IDs in `wiki/projects/kivora.md` (`claude-sonnet-4-5-20250929` / `claude-haiku-4-5-20251001`) reflect the project's actual runtime configuration — accurate as project state, not stale wiki content.

---

## 3. Missing implied concept pages

The following concepts are mentioned across multiple project pages and their log entries flag them as candidates for promotion. None have been auto-created because the schema requires human opt-in for new concept pages.

### M1 — `data-contract-evolution` (strong candidate — ready)

**Evidence**: Cited in two Kivora log entries (2026-05-17, 2026-05-19) as *"strongest data-contract-evolution worked example in the wiki."* ADR-0002 (the two-column lifecycle/assessed-status model) is the concrete artifact. The 2026-05-19 log elevated this to the top of the Related-concepts candidate list on the Kivora page.

**Why now**: Kivora's ADR-0002 is a well-documented, self-contained worked example. The concept is broad enough to apply to any SaaS that ships an enum-backed status field and later needs to split it. Generalizes across Kivora (Finding status) and any future project that ships a similar migration.

**Suggested scope**: additive migration pattern for data contracts where the new shape is incompatible with the old one; two-column resolution as one strategy; distinguish from [[dual-write-rollout]] (which handles the *pipeline* side) and from schema evolution generally.

### M2 — `multi-repo-wrapper` (strong candidate — 5+ examples)

**Evidence**: The pattern appears in five active projects — Kivora, Glydr, Helm, Brolly, and Vedge — all using the same shape: an untracked workspace root container holding N git-managed subrepos, each with independent branches/CI/deploys. Multiple log entries flag it as worth tracking ("matches Vedge", "same shape as Helm/Brolly", "three projects now use this pattern").

**Why now**: Five projects is past the "second-example" threshold the schema uses for concept promotion. The pattern has practical implications (workspace root items stay unversioned, BRAIN.md gitignored, per-subdir CI) and a reusable set of tradeoffs worth capturing once and citing.

**Suggested scope**: define the shape (workspace container + N-subdir repos), compare to monorepo and conventional polyrepo, enumerate the tradeoffs (independent branches/deploys vs. cross-repo refactors), note the "workspace root as scratch space" sub-convention.

### M3 — `flag-audit-cadence` (candidate — 1 example, second expected soon)

**Evidence**: Crystallized in the Kivora 2026-05-19 log: *"every 5-6 waves an explicit audit pass that catches latent P0/P1s within bounded time (W22 cache-eviction, W27 per-tenant arch gap → V106, W32 read-path undercount → W33)."* The log notes it *"could promote to concept pages if a second project shows the same pattern."*

**Hold or promote**: hold until a second project exhibits the same practice. Flag here so the next Kivora or Glydr update can note if the pattern recurs.

### M4 — `port-based-cutover` (candidate — 1 example)

**Evidence**: Crystallized in the Kivora 2026-05-19 log: *"port-based abstraction for changing-source reads"* — the `ControlStatusSourcePort` interface that let Kivora flip the authoritative source of truth without touching callers. Related to but distinct from [[reasoning-execution-split]] (which is about LLM vs. code) and [[dual-write-rollout]] (which is about write paths).

**Hold or promote**: hold until a second example. The concept is clear but one worked example is a thin basis.

### M5 — `two-column-resolution` (candidate — 1 example)

**Evidence**: Crystallized in the Kivora 2026-05-19 log: *"two-column resolution for genuinely-different-fact enums"* — the ADR-0002 Path 3 pattern where a single status field is split into `lifecycle_status` + `assessed_status` when the two values represent different facts rather than different states of the same fact.

**Hold or promote**: closely related to M1 (`data-contract-evolution`). Could be a sub-section of that concept page rather than its own page. Hold for the data-contract-evolution concept page creation decision.

---

## 4. Long-standing stubs (informational, no action required)

The script counts 222 stubs among 455 wiki pages. The bulk are from the 2026-05-02–05-05 design-system ingest batches (branded products and aesthetic-style entities). These are expected stubs — the source content is all in `raw/open-design/` and `raw/<brand>/DESIGN.md`; stubs resolve when a project needs a specific brand's detail. No expansion recommended at this time.

Notable long-standing stubs that could warrant attention if a primary source becomes available:
- `wiki/entities/boris-cherny` — Anthropic Claude Code engineer, cited for an unverified HN claim about silently-lowered thinking effort. The claim is marked unverified; no action unless a primary source appears.
- `wiki/entities/cowork` — Anthropic's Claude Code mode; has substantial coverage from Guri Singh + Khairallah but the stub tag was never removed. Promote to substantive if useful.

---

## 5. Schema notes

No schema issues found. All page types, required fields, and structural conventions are intact.

---

*Lint run: 2026-05-25. Report written by the LLM maintainer. Human review required for items in §§ 1, 3.*
