---
type: project
title: Clarvyn
created: 2026-05-09
updated: 2026-07-22
status: active
repo: multi-repo, migrated 2026-07-16 to github.com/kobbyopoku/clarvyn-{backend,agent,portal,landing} (default branch `staging`); mobile app remote DELETED (local-only on branch `phase2-execution`, no new remote yet); outer wrapper has no remote
local_path: /Users/kobbyopoku/ROAM/CascadeProjects/clarvyn
stack: [java-21, spring-boot-3.3.5, python-3.11, fastapi, anthropic-claude, pgvector, postgres, postgres-rls, react-19, vite-rolldown, typescript, tailwind, react-native, expo-50, zustand, tanstack-query, liquibase, docker-compose, railway, vercel, stripe, prometheus, grafana]
started: 2026-01-15
owner_org: roam-labs
affiliation: roam-labs-product
aliases: [clarvyn-platform, clarvyn-hr]
tags: [project, hr, hris, ai-agent, agentic, multi-tenant, saas, startups, claude]
---

> **Lineage**: owned product of [[wiki/entities/roam-labs|ROAM Labs]] ([[wiki/entities/godwin-opoku-duah|Godwin Opoku Duah]], founder). Commercial / IP-bearing — sister architecture to [[wiki/projects/kivora|Kivora]] (same Spring Boot + FastAPI Claude + pgvector RAG stack, different domain).

# Clarvyn

> AI-first HR platform for startup founders — the agent *is* the HR department, autonomously running payroll, reviews, hiring, onboarding, and 1:1 employee check-ins across a Spring Boot HRIS, a FastAPI Claude agent, a React portal, and a React Native mobile app.

## What and why

Clarvyn replaces "HR software for HR people" with "an HR department that happens to be software." Founders of 5–50-person startups sign up, connect their tools (Slack, Jira, calendar, payroll), and the Claude-powered agent runs HR — proactively, not reactively. The HRIS (Spring Boot + Postgres) is the data backbone; the agent (FastAPI + Claude Sonnet + pgvector RAG) is the primary interface. Founders use the portal for oversight; employees use the mobile app to talk to "HR" (the agent). The agent serves both audiences with role-aware system prompts: founder mode (full access, write actions) and employee mode (own data, policies, requests, anonymous complaints). Success is "the founder gets updates, not tasks."

## Stack and infrastructure

- **Backend API (`backend/`)**: Spring Boot 3.3.5 / Java 21 / Maven. PostgreSQL + pgvector. Liquibase (~99 changesets). JWT (HS512, 24h; token returned in the `Authorization` **response header**, held in-memory + sessionStorage) + BCrypt + 5-role RBAC (PLATFORM_OWNER → WORKSPACE_OWNER → MANAGER → HR_STAFF → EMPLOYEE) with 38 permissions. Multi-tenant via `companyId` scoping **now backed by Postgres RLS** (Phase 2 enforced in prod 2026-07-20 — backend connects as non-superuser `clarvyn_app`, connection-level GUC stamps company, BYPASSRLS revoked). Standardized `BaseResponse<T>` envelope and `BaseEntity` audit base; `AuditEvent` audit trail with before/after snapshots. Also hosts the **billing/subscription** layer (Free/Pro/Custom plans, 13 gateable modules via `@RequiresModule`, token-usage enforcement 80% alert → 100% block) and **admin user management** (`/admin/*`). Port 8089, package `com.clarvyn.platform`. Repo: `github.com/kobbyopoku/clarvyn-backend`.
- **AI Microservice (`agent/`)**: FastAPI / Python 3.11. Anthropic Claude API: `claude-sonnet-5` for agent reasoning, `claude-haiku-4-5-20251001` for titles + compaction + guard checks (the `claude-*-4` IDs were **retired by the API in 2026-07**; the Claude 5 family also rejects the `temperature` param). Agentic loop `core.py` + `soul.py` + `memory.py` + `tools.py` + `tool_executor.py`, now **multi-agent**: an orchestrator soul "Clarvyn" delegates to 5 specialists — Ada, Nova, Penny, Sage, Scout (`agent/app/agents/souls/`) — via `delegate_to_specialist` → `run_specialist_task` (delegation depth capped at 1). **40+ tools**, role-gated by `get_tools_for_role()` and per-turn scoped by `scope_tools_for_turn()`; destructive ones pass a confirmation gate + an independent Verifier. **Self-learning**: reflect-and-remember after each privileged turn/specialist run (`app/agents/reflection.py`), pgvector recall gated by a human review-gate. SSE streaming → Java WebClient Flux → React `ReadableStream`. RAG: chunk (500 tokens, 50 overlap) → `sentence-transformers/all-MiniLM-L6-v2` (384-dim) → pgvector. APScheduler `CronTrigger` for proactive briefings + weekly memory consolidation/self-review. Agent-side tables via `Base.metadata.create_all()` + idempotent startup `ALTER`s (no Liquibase). Direct tools + Composio-based external connectors. Port 8090. Repo: `github.com/kobbyopoku/clarvyn-agent`.
- **HR Portal (`portal/`)**: React 19 + TypeScript + Vite (Rolldown). "Weightless" Tailwind design system, **mobile-first** (375px primary, ≥44px targets; BambooHR is the UX reference). Custom `ApiClient` (native `fetch`). React Context for state (Auth, Theme, FontSize). Vitest + MSW (~461 tests). Founder-facing surfaces include ClarvynAI chat (SSE, with a **History drawer** for past conversations), a **Team Memory** review queue, real Insights/Activity tabs, audit log, billing/admin. Port 5175. Repo: `github.com/kobbyopoku/clarvyn-portal`.
- **Landing Page (`landing/`)**: React 19 + TypeScript + Vite. Pricing page + module catalog + demo form. Port 5174. Repo: `github.com/kobbyopoku/clarvyn-landing`.
- **Mobile App (`app/`)**: React Native 0.73 + **Expo ~50** (not 54 — the earlier page was wrong). Zustand + TanStack Query v5. Axios with auto-refresh interceptor + Expo Push Notifications. Now at near-full employee portal parity, incl. an employee AI chat screen with message-feedback thumbs. **Remote deleted 2026-07-19** — code is local-only on branch `phase2-execution`; no `kobbyopoku/clarvyn-app` remote created yet.
- **Database**: `ankane/pgvector:latest` on host port 5433. Four agent-specific tables (`conversations`, `conversation_messages`, `document_chunks`, `scheduled_tasks`) on top of the HRIS schema.
- **Infra**: Docker Compose (`docker-compose.yml` + `docker-compose.monitoring.yml`), `clarvyn-network` (172.25.0.0/16), `start.sh` / `stop.sh`. GitHub Actions CI/CD. Prometheus + Grafana monitoring stack. Local-stack refreshes must always `docker compose build` (never a bare `restart` — it reuses stale images).
- **Deploy topology**: **Railway** hosts backend + agent; **Vercel** hosts portal + landing; both auto-deploy from each repo's `main`. Release rule is **staging-first** — work lands on `staging`, then a `--no-ff` merge to `main` ships to prod (never push straight to `main`). Prod DB is a Railway pgvector service; direct prod-DB access is restricted (the human runs any prod mutation, agent provides the command).
- **Outer wrapper**: `/Users/kobbyopoku/ROAM/CascadeProjects/clarvyn` is *not* a git repo — it's a working directory holding the five service repos. Same topology as [[wiki/projects/stacesprouts|StaceSprouts]] and [[wiki/projects/coffee-break-with-big-sis|Coffee Break With Big Sis]].
- **Brain pointer drops**: this multi-repo project carries `BRAIN.md` in **six** locations — the wrapper plus each of the five service repos (`backend/`, `agent/`, `portal/`, `landing/`, `app/`) — so any agent session opened in any service finds the brain page immediately. `BRAIN.md` is in each service's `.gitignore` (lines 6 / 80 / 27 / 27 / 28 respectively as of 2026-05-09); the wrapper's `.gitignore` ignores it as well (no-op for git since the wrapper isn't a repo, but respected by ripgrep/find). All six pointer files reference the same brain page.

## Current focus

Week of 2026-07-22 — the theme is **agent trust + the self-learning loop**, on top of a spring of platform build-out:

- **Agent safety framework shipped to prod (2026-07-22)** — 5 hardening levers assessed against a personal framework and built in sequence: (1) an independent **Verifier** before 17 destructive/approval tools (fail-safe); (2) input/output **guardrails** (input injection block pre-LLM, output async-flag; fail-open); (3) per-turn **tool-scoping** (~89 → ~20 tools; fail-open); (4) a **review-gate** on auto-learned memory; (5) a **hardened harness** (retry transient tool failures with idempotency care + a `get_recent_agent_actions` episodic-recall tool). 616 agent tests, golden evals green. See [[wiki/concepts/fail-open-vs-fail-safe-gating]] and [[wiki/concepts/review-gate-on-autonomous-learning]].
- **Self-learning review + feedback loop closed end-to-end (2026-07-22)** — founder **memory review queue** (approve/dismiss auto-extracted memories before they enter recall), **employee chat thumbs** on portal + mobile (recorded but non-teaching by construction), and a **founder feedback rollup** in Insights (employee approval rate + recent flagged replies). Smoke-tested against real Postgres through both the agent-direct and portal→backend→agent paths.
- **ClarvynAI UX** — founder conversation list demoted from a persistent sidebar to an on-demand **History drawer**; entry now always resumes the most recent conversation (fixed a regression where a >24h-old thread opened blank once the sidebar that had masked it was removed).
- **Broader arc since May** (all in prod): **multi-agent orchestration** (Clarvyn + 5 specialists), **RLS Phase 2** enforced, end-to-end **billing/subscription + token enforcement** + module gating, **admin user management** + Platform Admin oversight, **termination workflow** (login deactivation + billing exemption + offboarding), **agent-led onboarding** (first login = proactive setup chat, not a wizard), **26 agent config write-tools** (the agent now owns HR setup/config), **audit logs** (user + agent) with before/after, and **Composio** external-connector marketplace.
- **Still open**: real 2-way payroll integrations (Gusto/Deel); a founder-visibility rollup for *aggregated* employee feedback (this session flagged as next); Stripe **payment collection** remains the one blocked billing slice (no test keys); recreate a mobile-app remote.

## Architecture decisions

- **Pre-2026-01-15 (initial scaffold)** — **Five services, one product**. Backend (HRIS) + Agent (FastAPI) + Portal (founder oversight) + Landing + Mobile (employee-facing). Each is a separate git repo; the wrapper has no remote. Rationale: independent deploy cadence per surface; portal and mobile are different audiences and need different release rhythms.
- **Pre-2026-01-15** — **Single backend package `com.clarvyn.platform`** with `BaseResponse<T>` envelope and `BaseEntity` audit base. Liquibase as schema authority. **Multi-tenant by `companyId` column scoping** (not schema-per-tenant — diverges from sibling [[wiki/projects/coffee-break-with-big-sis|Coffee Break With Big Sis]] which uses schema-per-tenant). Rationale: simpler operational model, single connection pool, easier cross-company analytics for the platform owner.
- **Pre-2026-03 (initial agent build)** — **Agent loop is SOUL + Tools + Memory** (`core.py` + `soul.py` + `memory.py` + `tools.py`). Worked example of [[wiki/concepts/agentic-loop]]. [[wiki/concepts/reasoning-execution-split]] applied: Sonnet drives the loop, Haiku handles titles + memory compaction at the 50-message threshold.
- **Pre-2026-03** — **RAG is in-process pgvector**, not a separate vector DB. Embed with `sentence-transformers/all-MiniLM-L6-v2` (384-dim). Rationale: one less moving piece in Compose; pgvector is "good enough" until the corpus grows past Postgres single-node comfort.
- **2026-03 (Wave 5C — Agent Evolution)** — **Agent gets write access**. Until Wave 5C, every agent tool was read-only; Wave 5C added 13 write tools through `hr_action_service` + a privileged `InternalAgentController`. Rationale: the product thesis (agent-as-HR-department) requires actions, not just answers — approve leave, process payroll, create reviews, file complaints.
- **2026-03 (Wave 5B)** — **Role-aware agent personas**. Three system prompts (founder / manager / employee) + per-role tool filtering at the `tool_executor` layer. Rationale: same agent must safely serve a CEO and an employee from the same codebase without cross-tenant or cross-role data leakage.
- **2026-03 (Wave 5D)** — **Anonymous feedback mode**. Complaints stored without `user_id`, only category + sentiment kept; pattern-detection alerts fired to founder when threshold tripped. Rationale: psychological safety as a first-class product feature, not an afterthought.
- **2026-04/05 (Wave 6E)** — **MCP server + client layer for per-company external integrations**. `agent/app/mcp/server.py` exposes Clarvyn's HR tools as an MCP server; `agent/app/mcp/client.py` consumes external MCP tool servers; `McpConnection` model registers per-tenant connections; `tool_executor` routes external MCP calls. Rationale: avoid bespoke per-integration glue code as the integration surface grows (Slack/Jira/GCal/Gusto/Deel/job boards). Direct integrations stay where vendors don't speak MCP yet.
- **March 2026** — **`agentJ` retired**. Earlier Java-based agent prototype removed; the Python `agent/` is the single agent codebase.
- **2026-07 (multi-agent turn)** — **orchestrator + specialists**. The single agent became an orchestrator soul "Clarvyn" that delegates to 5 domain specialists (Ada/Nova/Penny/Sage/Scout) via `delegate_to_specialist`, depth-capped at 1. Second worked example in the brain of [[wiki/concepts/multi-agent-delegation-with-verifier]] (the first is [[wiki/projects/kivora|Kivora]]). Rationale: domain focus per specialist + a clean seam for the Verifier to gate destructive side-effects.
- **2026-07-17 / 2026-07-22 (self-learning + review-gate)** — **the agent learns, but behind a human gate**. Reflect-and-remember writes pgvector memories after privileged turns; auto-extracted ones are quarantined (`reviewed=false`, excluded from recall) until a founder approves them in Team Memory; founder-authored/thumbs lessons skip the queue. Employees/anonymous NEVER teach — enforced server-side by role. Worked example of [[wiki/concepts/review-gate-on-autonomous-learning]]. Rationale: keep the compounding benefit of learning while capping silent competence drift.
- **2026-07-22 (agent safety framework)** — **failure direction is chosen per gate**. Input/output guards + tool-scoping fail **open** (an outage must not lock users out); the Verifier before destructive tools fails **safe** (no destructive action on a coin-flip; no silent bypass — a review pass caught 4 tools that had escaped both Verifier and confirmation). Worked example of [[wiki/concepts/fail-open-vs-fail-safe-gating]]. Also: strict `allow is True` verdict parsing, since `bool("false")` is truthy.
- **2026-07-20 (RLS Phase 2)** — **tenant isolation enforced at the database**, not just in query code. Backend connects as non-superuser `clarvyn_app`; a connection-borrow GUC stamper sets the company per request; 26 child-table `company_id` sync triggers; BYPASSRLS revoked. `companyId` column-scoping remains but is now belt-and-suspenders. (H2 masks RLS + untyped-param bugs — see Lessons.)
- **2026-07 (billing + entitlements)** — **per-module feature gates + token enforcement**. Free/Pro/Custom plans, 13 gateable modules enforced both portal-side and backend-side (`@RequiresModule`, grandfather rule), token-usage 80% alert → 100% block. Stripe foundation (Checkout/webhooks/invoices) built; **payment collection blocked** on missing test keys.
- **2026-07-16 (repo migration + release discipline)** — repos moved to `github.com/kobbyopoku/clarvyn-*`; default branch is `staging`; prod ships via `--no-ff` merge `staging → main` (Railway/Vercel auto-deploy from `main`). Never push straight to `main`. Mobile app remote was deleted 2026-07-19 and not recreated — mobile code is local-only.
- **2026-07 (model migration to Claude 5 family)** — reasoning moved to `claude-sonnet-5`, fast tasks to `claude-haiku-4-5-20251001`; the `claude-*-4` IDs were retired by the API and the Claude 5 family rejects the `temperature` param. Both overridable via `CLAUDE_MODEL_*` in Compose.

## Open questions

- **Real 2-way payroll integrations** — Gusto / Deel are named in the vision but not built. Build order vs which integration is the wedge?
- **Google Calendar two-way sync** — webhook receiver landed in 6C; full bidirectional sync (creating events from agent decisions) still open.
- ~~**Portal Activities + Insights tabs** — partially mocked.~~ **Resolved (2026-07):** both tabs are now real data (Activity = `agent_task_runs` + tool calls; Insights = sentiment/query/conversation stats + the new employee-feedback rollup). No mock segments left.
- **Outer wrapper remote** — should `clarvyn/` itself have a private GitHub remote for cross-cutting commits, or stay local-only? Same question as [[wiki/projects/stacesprouts|StaceSprouts]].
- **MCP credential rotation per tenant** — per-company MCP credentials stored where, rotated how, with what audit trail?
- **Mobile push delivery reliability** — Expo Push works for dev; need a fallback for users who disable Expo notifications or are on dev clients without `projectId` set.
- **Anonymous feedback pattern-alert thresholds** — currently fixed; should they be per-company configurable, and how is the founder educated about what triggers an alert?
- **Performance review conducted-via-chat UX** — mobile chat is the surface, but the "conducting a review through conversation" pattern hasn't been studied for completion rate vs a structured form fallback.
- **Mobile app remote** — the `clarvyn/app.git` remote was deleted (2026-07-19); code is local-only on `phase2-execution`. When to `gh repo create kobbyopoku/clarvyn-app` and push? (Same "no remote" question the outer wrapper has.)
- **Founder-visibility rollup for *aggregated* employee feedback** — the raw employee thumbs are now founder-visible in Insights, but a dedicated summarized view (trends, top themes over time) is flagged as the next build.
- **Stripe payment collection** — the only blocked billing slice: Checkout/webhook/invoice code exists but can't be exercised without Stripe test keys. When do the keys land?
- **External connector credential rotation** — Composio now brokers external connectors per company (superseding the earlier bespoke-MCP-credential question); rotation/audit story across tenants still worth pinning down.

## Lessons learned

Project-specific; promote to `wiki/concepts/` when patterns generalize.

- **Read-only agents disappoint.** Wave 5C was a step-change in product perception once the agent could *do* (approve leave, file a review) instead of only *answer*. Agentic products plateau at low value until they get write access; a read-only agent is a chatbot wearing an agent costume. *Candidate for promotion to a concept page if it recurs in [[wiki/projects/kivora|Kivora]] or future agent projects.*
- **Role-aware system prompts are not just a prompt change.** They require parallel changes in the tool-executor's allow-list and the tool-call response shape (an employee asking "show me everyone's salary" should get a polite refusal *from a tool*, not a model-level evasion that can be jailbroken). Defense-in-depth: prompt + tool gate + data-layer scope.
- **Reasoning-execution-split (Sonnet/Haiku) pays off at the memory-compaction boundary.** Compaction is the highest-volume LLM call in a long conversation; using Haiku there cuts cost without a quality hit because the task is summarization, not reasoning. Direct application of [[wiki/concepts/reasoning-execution-split]].
- **MCP is the right abstraction once external integrations exceed ~3.** Below that, direct tools are simpler. Wave 6E was triggered exactly when Slack + Jira + GCal + Teams + Discord all needed connectors and the per-integration glue was about to duplicate. *Candidate for promotion to a concept page — "MCP threshold" — if it holds across other agent projects.*
- **In-process pgvector beats a separate vector DB until the corpus forces it.** One Postgres, one Compose service, one connection pool — and the embeddings sit next to the rows they describe. Same conclusion reached independently in [[wiki/projects/kivora|Kivora]].
- **Choose each gate's failure direction from its blast radius, not globally.** Guards on availability fail-open; gates on destructive actions fail-safe — and the fail-safe gate must have no silent bypass (a review pass caught 4 destructive tools escaping both the Verifier and the confirmation gate). Promoted to [[wiki/concepts/fail-open-vs-fail-safe-gating]].
- **A self-learning agent needs a review-gate or it drifts silently.** Auto-authored memory changes future behavior with no code/prompt change; quarantine machine-proposed lessons from recall until a human ratifies them, and gate *who may teach* server-side from identity so untrusted actors have no route to the learning loop. Promoted to [[wiki/concepts/review-gate-on-autonomous-learning]].
- **Removing a UI affordance can expose behavior it was silently compensating for.** Demoting the ClarvynAI sidebar to a drawer looked cosmetic, but the always-visible list had been masking a 24h "resume last chat" cutoff — remove the list and a stale thread opens blank, read by the user as data loss. Before deleting a persistent UI element, grep the state/effects it touched and ask "what did its presence make OK?"; verify data integrity first (`SELECT count(*)`) and say so.
- **H2 masks Postgres-only bugs — verify DB-layer changes on real Postgres.** RLS behavior and untyped-parameter bugs (`(:p IS NULL OR col=:p)` leaves the param untyped on Postgres, tolerated by H2) both passed the H2 test suite and 500'd in prod. The audit-log 500 this era was exactly this; fix required a `CAST(:p AS ...)`. Recurring enough to be a standing rule, not a one-off.

## Related

- **Concepts**: [[wiki/concepts/agentic-loop]] (SOUL+Tools+Memory pattern), [[wiki/concepts/multi-agent-delegation-with-verifier]] (orchestrator Clarvyn + 5 specialists + Verifier — 2nd worked example after Kivora), [[wiki/concepts/fail-open-vs-fail-safe-gating]] (2026-07 safety framework), [[wiki/concepts/review-gate-on-autonomous-learning]] (self-learning behind a founder gate), [[wiki/concepts/augmented-llm]], [[wiki/concepts/reasoning-execution-split]] (Sonnet for loop, Haiku for compaction + guards), [[wiki/concepts/progressive-disclosure]] (per-turn tool-scoping), [[wiki/concepts/retrieval-augmented-generation]] (in-process pgvector), [[wiki/concepts/mcp-server]] (external integrations, now via Composio), [[wiki/concepts/scheduled-automation]] (APScheduler proactive briefings + consolidation), [[wiki/concepts/services-as-software]] (HR-as-agent thesis), [[wiki/concepts/agent-workflow-patterns]].
- **Entities**: [[wiki/entities/anthropic]] (Claude Sonnet/Haiku), [[wiki/entities/huggingface]] (sentence-transformers, RoBERTa, BART), [[wiki/entities/expo]] (mobile + Expo Push), [[wiki/entities/slack]] (Wave 6C bot), [[wiki/entities/gusto]] (named future integration target).
- **Other projects**: [[wiki/projects/kivora]] — sibling architecture (Spring Boot + FastAPI Claude agent + pgvector RAG); the closest pattern-match in the brain — different domain (GRC vs HR), same agent stack. [[wiki/projects/vedge]] — multi-tenant SaaS reference. [[wiki/projects/stacesprouts]] and [[wiki/projects/coffee-break-with-big-sis]] — same "outer wrapper, no remote, multiple service repos" topology.

## External links

- **Repo (backend)**: https://github.com/kobbyopoku/clarvyn-backend (Railway; default branch `staging`)
- **Repo (agent)**: https://github.com/kobbyopoku/clarvyn-agent (Railway; default branch `staging`)
- **Repo (portal)**: https://github.com/kobbyopoku/clarvyn-portal (Vercel; default branch `staging`)
- **Repo (landing)**: https://github.com/kobbyopoku/clarvyn-landing (Vercel; default branch `staging`)
- **Repo (mobile app)**: remote **deleted** — local-only on branch `phase2-execution`; `kobbyopoku/clarvyn-app` not yet created.
- **Project's own `CLAUDE.md`**: `/Users/kobbyopoku/ROAM/CascadeProjects/clarvyn/CLAUDE.md` (Plan-Mode-Default + Subagent Strategy + Self-Improvement Loop + Verification + Demand Elegance + Autonomous Bug Fixing operating instructions).
- **Auto-memory**: `~/.claude/projects/-Users-kobbyopoku-ROAM-CascadeProjects-clarvyn/memory/MEMORY.md` (and `architecture.md` companion).
- **Project docs**: `/Users/kobbyopoku/ROAM/CascadeProjects/clarvyn/docs/` (14+ markdown files — `PROJECT_OVERVIEW.md`, `PROJECT_ASSESSMENT.md`, `DOCKER_GUIDE.md`, etc.).
- **Default platform-owner credential** (dev only): `godwin@clarvyn.ai` / `Admin@123` (seeded by `DataInitializer`).
